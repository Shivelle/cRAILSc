Build a program that: 

- Reads in user information that is provided
- Reads in permissions froma a json file
- Creates a new user with the assigned permissions
- Saves the user in a users listing file. If file doesn't exist yet, it creates one.